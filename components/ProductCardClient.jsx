"use client"
import React from 'react'

const ProductCardClient = ({ children }) => {
  return (
    <ProductCardClient>
      {children}
    </ProductCardClient>
  )
}

export default ProductCardClient